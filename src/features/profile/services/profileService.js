import { profileAPI } from '../../../shared/services/apiClient'

/**
 * Profile service - uses the centralized profileAPI from apiClient
 * Handles user profile data, fitness profile, and photo uploads
 */
export const profileService = profileAPI

export default profileService
